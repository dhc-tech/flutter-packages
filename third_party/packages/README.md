# third_party/packages

This directory is where third-party/forked packages would live, matching
`flutter/packages`' repository layout. The repository tooling
(`flutter_plugin_tools`) expects this path to exist (it diffs
`packages/` and `third_party/packages/` together to detect changes), so
this placeholder keeps that working even while this repo has no
third-party packages of its own.
