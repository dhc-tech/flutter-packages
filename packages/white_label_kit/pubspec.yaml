name: white_label_kit
description: >-
  A complete multi-tenant white-label and flavor management toolkit for Flutter
  apps. Automatically configures Android Gradle and iOS Xcode schemes from a single YAML file.
version: 0.0.4
homepage: https://github.com/dhc-tech/flutter-packages/tree/main/packages/white_label_kit
repository: https://github.com/dhc-tech/flutter-packages
issue_tracker: https://github.com/dhc-tech/flutter-packages/issues?q=is%3Aissue+is%3Aopen+label%3A%22p%3A+white_label_kit%22
topics:
  - white-label
  - flavors
  - multi-tenant
  - cli
  - build-tools

resolution: workspace
environment:
  sdk: ^3.13.0

executables:
  white_label: white_label
  menu: menu
  runner: runner
  init: init
  configure: configure
  generate: generate
  list: list
  add-tenant: add_tenant
  update-tenant: update_tenant
  remove-tenant: remove_tenant
  doctor: doctor
  validate: validate
  run: run
  build: build

dependencies:
  yaml: ^3.1.3
  path: ^1.9.1
  build: ^4.0.10

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
  build_runner: ^2.16.0
  build_test: ^3.5.19
