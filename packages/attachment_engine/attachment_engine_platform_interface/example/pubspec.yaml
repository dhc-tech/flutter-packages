name: attachment_engine_platform_interface_example
description: "Example demonstrating how platform implementations register against attachment_engine_platform_interface."
publish_to: 'none'

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  attachment_engine_platform_interface:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
