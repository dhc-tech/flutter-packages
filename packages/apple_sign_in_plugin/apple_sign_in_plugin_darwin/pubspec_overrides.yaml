# Local development override — remove before publishing.
# apple_sign_in_plugin_platform_interface is not yet on pub.dev; this
# points at the sibling package in this monorepo so local builds/tests
# resolve correctly.
dependency_overrides:
  apple_sign_in_plugin_platform_interface:
    path: ../apple_sign_in_plugin_platform_interface
