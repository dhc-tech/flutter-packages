name: apple_sign_in_plugin_platform_interface_example
description: "Example app demonstrating how a platform implementation registers itself against apple_sign_in_plugin_platform_interface."
publish_to: 'none' # This package is not intended for publishing.

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  apple_sign_in_plugin_platform_interface:
    path: ../
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
