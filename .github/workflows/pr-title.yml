# Copyright 2026 DHC Tech
# Use of this source code is governed by an MIT-style license that can be
# found in the LICENSE file.
#
# Enforces Conventional Commits (https://www.conventionalcommits.org/) on
# the PR title, which this repo's release tooling and PR classification
# both rely on as the source of truth (no separate commit-message check,
# to avoid duplicating validation).

name: PR Title

on:
  # pull_request_target is used (instead of pull_request) so this check can
  # post a status on PRs from forks, where the default pull_request token is
  # read-only. This is safe here: the job only reads PR title metadata via
  # the GitHub API, and never checks out or executes PR code.
  pull_request_target:
    types: [opened, edited, synchronize, reopened]

permissions:
  pull-requests: read
  statuses: write

jobs:
  pr-title:
    name: pr-title
    runs-on: ubuntu-latest
    steps:
      - name: Validate PR title
        uses: amannn/action-semantic-pull-request@48f256284bd46cdaab1048c3721360e808335d50 # v6.1.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            feat
            fix
            docs
            style
            refactor
            perf
            test
            build
            ci
            chore
            revert
          requireScope: false
